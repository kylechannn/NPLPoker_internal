# NPL Poker Internal — UI / 布局分析

> 分析对象：`ui/` 前端（React + TypeScript + Vite）。重点：`App.tsx`（外壳）、`desk/TimerDisplay.tsx`（房间时钟）、`styles.css`（~6200 行全局样式）。

---

## 1. 技术栈与架构

- **React 18 + TypeScript + Vite**，图标用 `lucide-react`，二维码用 `qrcode.react`。
- 桌面应用：由 Go host 包一个无边框 webview，窗口按钮（最小化/最大化/关闭）、拖拽都是自绘（`window.nplWindow*` 桥接）。
- **按标签懒加载**：六个工作区（Tournament / Cash / Registrations / Players / Jackpot / Export）各自 `lazy()` 分包，只有打开才加载 CSS 和代码——这点做得好，启动包更轻。
- 状态：本地 `localStorage` 存 venue、operator 会话（12 小时班次过期）。

## 2. 整体外壳布局（App.tsx）

- **左侧固定 248px 边栏 + 右侧主区**（CSS grid）。边栏自上而下：品牌标识 → 网络信号条 → 后端连接灯 → 可滚动导航（Operations / Club / Reports 三组）→ 会话二维码 → 底部操作员卡片。
- **顶栏**：场馆选择器、手动更新、同步头像、通知铃、窗口按钮。
- **主区**：页面标题 → 工作区内容 → 底部状态栏（本地服务健康点 + 构建版本）。
- **视觉语言**：深海军蓝（void/midnight）暗色主题，青（cyan）+ 金（gold）+ 绿（green）点缀，大量 `clip-path` 斜切角、发光渐变、微动画，显示字体 Bahnschrift，标签用等宽字体——整体是"科幻指挥台"风格，完成度很高。

## 3. 房间时钟（TimerDisplay.tsx）

时钟有两套形态：

- **Mini（白卡 "Sichuan" 样式）**：等级药丸、盲注、倒计时、进度条、上一/下一级 + 运行按钮。紧凑、干净。
- **Max（投影大屏）**：巨大的红色霓虹表盘（纯 SVG，十几个渐变 + 光晕 + 走秒头灯）、左列统计卡（当前时间/下次休息/人数/平均筹码/rebuy/add-on）、中心表盘（等级/倒计时/盲注/下一级）、右栏每 10 秒在"筹码面额 ↔ 奖金分配"之间切换、底部自动隐藏的控制条（info ↔ controls，3 秒空闲回落）。服务器权威计时，本地每秒 tick、每 5 秒重新对表——防漂移做得很稳。

---

## 4. 可改进点

### A. 过度装饰 vs. 场馆硬件
表盘和外壳堆了非常多渐变、光晕、`backdrop-filter: blur()`、动画层。代码注释自己都提到过"venue laptops with weak GPUs"的重绘成本担忧。在低端机上，Max 时钟的多层 SVG 描边 + 光晕会吃 GPU。**建议**：给弱机或 `prefers-reduced-motion` 提供一档"精简渲染"（去掉 halo/glass/头灯光晕，保留主弧线），视觉几乎无损但省重绘。

### B. 字号偏小、对比偏弱
标签大量使用 `0.5rem / 0.48rem` 等极小等宽字，颜色 `--npl-faint (#5f7699)` 在深色上对比很低。前台高压环境下需要"扫一眼就懂"，部分关键信息（如状态栏、连接条副标签）偏难读。**建议**：把关键标签下限提到 ~0.62rem，faint 色只用于纯装饰性文字。

### C. 时钟与外壳的视觉语言不统一
外壳是 cyan/gold/navy，Mini 时钟是白底"Sichuan"卡，Max 时钟是红色霓虹主色——三种气质。投影用红色为了远处可读，可以理解，但 Mini 白卡和整体暗色调差异较大，切换时略跳。**建议**：让 Mini 卡也走暗色玻璃质感，或至少统一强调色，保留 Max 的红色特例即可。

### D. Max 控制条可发现性低
控制条 3 秒后自动隐藏，只有"点一下屏幕"才出现——投影场景合理，但新操作员不知道能点。**建议**：首次进入 Max 时给一个短暂的"点击屏幕显示控制"提示，或底部常驻一个极窄的把手。

### E. Overview 信息层级可更"仪表盘化"
Overview 目前是纵向堆叠：即将开始的场次 → CD-Key 授权卡 → 小票设置。授权卡和小票设置属于"设置类"，和"今晚运营"混在一起。**建议**：把运营信息（场次、人数、健康）放顶部密集网格，授权/小票折到次级或单独"设置"入口。

### F. 措辞与一致性
"Notification session"这类命名略生硬；顶栏两个大按钮（Manual update / Sync avatars）常驻占位较重，可考虑收进一个"同步"菜单，给主操作让空间。

### G. 移动端残留
外壳里有汉堡菜单、`nav-backdrop`、`sidebar--open` 等移动端逻辑，但这是桌面控制台。如果确定不会在窄屏用，这部分是可清理的死代码；如果 admin 手机端会用，则需要真正测过窄屏。

---

## 5. 优先级建议

| 优先级 | 项 | 收益 |
|---|---|---|
| 高 | A 弱机精简渲染档 | 直接影响现场流畅度 |
| 高 | B 关键字号/对比 | 前台可读性 |
| 中 | D Max 控制条提示 | 降低操作困惑 |
| 中 | C 视觉语言统一 | 品质感 |
| 低 | E/F/G 结构与清理 | 长期维护 |
